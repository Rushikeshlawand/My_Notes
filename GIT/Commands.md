1. git init: Initializes a new Git repository in the current directory.
2. git add .: Stages all changes in the current directory for the next commit.
3. git commit -m "Initial commit": Commits the staged changes with the message "Initial commit".
4. git remote add origin <URL>: Adds a remote repository named origin with the specified URL.
5. git push -u origin master: Pushes the changes to the master branch of the remote repository and sets it as the default upstream branch.

-m stands for message 
-u stands for upstream

6. git checkout -b "name_of_branch": This command creates a new branch and switches to it.
7. git branch: This command lists all branches available in the repository.
8. git checkout "branch_name": This command switches to the specified branch.
9. git merge "branch_name": This command merges the specified branch into the currently active branch.
10. git log : details of commit
11. git revert (Commit id) : roll back to the given commited id
12. git diff (1st C id) (2nd C id) : give diff  btw 2 commit